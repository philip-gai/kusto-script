.create-or-alter function with (
    docstring = 'Returns a list of awesome people',
    folder='Functions'
)
GetPeople()
{
    let people = datatable(FirstName: string, LastName: string, GitHubLogin: string)
    [
        "Philip", "Gai", "philip-gai"
    ];
    people
}
